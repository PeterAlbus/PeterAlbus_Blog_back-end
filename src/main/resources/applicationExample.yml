sa-token:
  # Token 名称，同时也是 cookie 名称和前端使用的请求头名称。
  token-name: satoken_peteralbus_blog
  # 是否允许同一账号并发登录。
  is-concurrent: true
  # 同一账号并发登录时是否共用 token。
  is-share: true
  # Token 生成风格。
  token-style: uuid
  # 是否输出操作日志。
  is-log: false
  # 无操作过期时间，-1 表示不限制。
  active-timeout: -1
